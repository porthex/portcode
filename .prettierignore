# Build output
dist/
dist-ssr/
web-dist/
coverage/

# Rust build + generated bindings
src-tauri/target/
src-tauri/gen/

# Dependencies
node_modules/

# Generated / vendored
pnpm-lock.yaml

# Opt-in MCP example config — intentionally JSONC (has `//` comments documenting
# the Serena opt-in), so it is not part of the Prettier (JSON) baseline.
.mcp.json.example

# Transient planning / handoff artifacts (not part of the formatted baseline)
HANDOFF-oss-setup.md
PHASE0-REPORT.md
docs/OSS-SETUP-PLAN.md
.claude/

# graphify-managed agent guidance — regenerated by `graphify install`, so keep it
# out of the prettier baseline (an upgrade must not churn the format check).
# `.claude/` above already covers the skill, hooks, and references.
CLAUDE.md

# graphify knowledge-graph output — machine-generated (graph.json / graph.html /
# GRAPH_REPORT.md / manifest.json). Committed for the team to share, but never
# part of the Prettier baseline: it is regenerated by `graphify update .` on
# every commit and must not churn `pnpm format:check`.
graphify-out/

# graphify research deliverable — kept as a reference artifact, not formatted.
GRAPHIFY_NOTES.md
