# Tester-facing beta build channel marker.
#
# Loaded only by Vite's `beta` mode. The native half is selected independently
# with Cargo's `beta-channel` feature so the UI identity and updater feed agree.
VITE_PORTCODE_CHANNEL=beta
